# OBS 音频检测助手

**OBS Audio Monitor Assistant** 是一款面向直播现场的 OBS 音频状态检测工具，用于持续监听指定音频源，并在音频长时间无有效电平时提醒主播、导播或现场运营人员及时处理。

该工具重点服务于电商直播、品牌直播、活动直播、访谈直播及其他对音频稳定性要求较高的直播场景。

## 项目定位

直播过程中，画面正常但麦克风无声是一类较难被现场人员第一时间发现的问题。常见原因包括无线麦克风电量耗尽、静音键误触、接收器断连、声卡路由异常、OBS 音频源选择错误或音源未处于活动状态。

OBS 音频检测助手通过连接 OBS WebSocket，读取 OBS 状态、输入源列表和目标音源电平。当目标音源在直播或录制期间连续低于设定阈值时，系统会先给出预警，再触发报警弹窗，帮助现场人员在观众反馈前发现并处理音频异常。

## 下载

最新版本可在 GitHub Releases 页面获取：

[下载 OBS 音频检测助手](https://github.com/Lin518-hub/obs-audio-monitor-assistant/releases/latest)

- Windows 用户下载 `OBS-Audio-Monitor-Assistant-Setup-*-x64.exe`。
- Mac 用户下载 `OBS-Audio-Monitor-Assistant-*-arm64.zip`，解压后运行其中的 `.app`。当前仅支持 Apple Silicon Mac，不支持 Intel x86 Mac。
- macOS 版本尚未进行 Apple Developer 签名和公证。若系统提示“文件已损坏”或无法打开，通常是 Gatekeeper 隔离标记导致，可在终端执行 `sudo xattr -cr /Applications/OBS\ 音频检测助手.app` 后再打开。正式商用分发建议配置 Apple Developer ID 签名与 notarization。

## V3.9.7 版本说明

- 客户端主进程日志落盘轮转，错误摘要脱敏后随状态上报，监控中心可查看最近错误。
- 主进程异常时采用有限次数自动重启保护，避免现场检测静默中断。
- 报警弹窗改为“确定 / 暂停检测”两种处理，移除旧的“单次忽略/延后检测”状态，旧历史记录仍兼容显示。
- 浮窗、报警窗与预警窗不再接收完整音量历史，减少界面刷新开销；报警提示音复用同一音频上下文。
- 远程桥接采用退避重连，服务器断线期间避免高频重试。
- 监控中心概览增加短缓存，状态数据每日自动备份并保留 14 份；服务器证书文件变化可热加载，无需重启容器。
- 直播间设备被移除时同步清理企业微信通知状态，避免重复注册后漏报。
- 保留客诉系统 `/complaint` 反向代理，更新与回归均验证通过。

## V3.9.6 版本说明

- 修复 Windows 小浮窗偶尔失去置顶，以及缩放时文字和圆角短暂变形的问题。
- 开发者模式可再次启用扫码监看与麦克风画中画。
- 监控后台可为指定直播间暂停企业微信报警与恢复通知。

## V3.9.5 版本说明

- 音频与机位报警统一使用同一套提醒节奏与外观，机位超时预警、报警与恢复通知保持对齐。
- 出镜机位可多选豁免，切到这些机位后不累计停留时间、不触发提醒。

## V3.9.4 版本说明

- 监控中心支持直接修改直播间名称，在线电脑会立即同步并保存，离线电脑在下次连接时自动同步。
- 名称同步增加修订号与冲突保护，旧客户端重连不会覆盖后台已经修改的新名称；电脑端后续手动改名仍可正常回写。
- 后台名称修改增加重复校验、操作记录和明确的在线同步状态提示。
- 企业微信通知首行直接显示直播间及异常类型，音频按本机报警时机推送，机位仅在连续超时 10 分钟后推送。

## V3.9.3 版本说明

- 修复设置折叠区域在字体加载、自动保存或窗口缩放后高度未及时更新，导致输入框和说明文字被裁切的问题。
- 软件更新统一为一个“检查更新”入口，自动按内部服务器、备用线路和 GitHub 顺序检测；Windows 在后台准备更新，并在退出或下次启动时静默完成安装。
- 已准备的安装包不再阻止后续版本检测，更新被替换、停用或完成后会清理客户端残留安装包。
- 修复客户端与监控服务器系统时间不一致时，电脑端有音频而监控中心仍显示“等待音频数据”的问题。
- 完成桌面端与远程服务器上线回归，保持 OBS、ATEM、开播检查、浮窗、监控中心及企业微信现有功能不变。

## V3.9.2 版本说明

- 电脑端仅保留直播间名称，线路状态、软件版本与企业微信测试统一由服务器监控中心维护。
- 监控中心改为“一直播间一行”，点击直播间后再查看完整状态与维护操作。
- 修复已安装版本与可用版本相同时仍显示需要更新的问题。
- 企业微信状态测试改由监控中心发起，并在对应群内提醒全部成员。

## V3.9.1 版本说明

- 监控中心改为随软件自动连接，无需开启手机远程访问；局域网可用时优先使用内部线路，外网环境自动回退到公网 HTTPS。
- 升级后仅重置一次旧直播间名称，启动时要求重新确认；每个直播间对应一台检测电脑，其他设置与本机 UUID 保持不变。
- 移除旧手机扫码、审批和移动控制链路，服务器同步清理历史手机授权。

## V3.9.0 版本说明

- 新增直播间集中监控中心，可在 `/monitor` 按直播间查看电脑在线状态、OBS 与 ATEM 状态、音频告警、软件版本和更新进度。
- 集中监控与手机扫码访问分离；关闭手机访问后，电脑仍可使用低开销状态上报和内部更新服务。
- 新增经过白名单限制的远程维护操作，包括显示软件、重连 OBS / ATEM、检查更新以及暂停或恢复检测；危险操作需要再次确认并保留审计记录。
- 每个直播间首次使用时需要填写名称，便于多台电脑在服务器端按直播间归组。
- 企业微信群机器人仅推送需要处理的音频告警和恢复通知，支持短时间合并与失败重试，减少重复消息。
- 内部更新服务增加私有 GitHub 仓库令牌支持、SHA-512 校验、两版本保留和发布完成后再切换元数据。
- 修复 Windows 已打开“投影 - 输出”等节目输出投影时，开播检查无法识别并保存窗口位置的问题。

## V3.8.1 版本说明

- ATEM 机位停留报警默认开启，默认阈值为 12 分钟；仅在直播、录制、虚拟摄像头或模拟开播期间计时。
- 机位超时复用音频报警的窗口样式、提示音和多屏策略，并使用独立机位文案；确认后重新计时，也可直接暂停检测。
- ATEM 断连、未开播或未读取到 PGM 时不会误报；移动监看中的音频状态不会再被机位报警覆盖。
- 直播机位统计仅在开发者模式显示，普通使用界面保持精简。
- 修复设置自动保存并发覆盖、引导完成时遗漏待保存字段，以及外部配置变化后界面未同步的问题。
- 修复关闭预警、清空历史后 ATEM 与手机远程状态短暂丢失的问题，并更新依赖安全版本。

## V3.7.0 版本说明

- ATEM 导播台转为正式功能，移除界面与文档中的 Beta 标识。
- 开播检查转为正式功能，无需开启开发者模式即可从主导航直接使用。
- 开机自动启动后直接进入开播检查，便于直播电脑完成开播前准备。
- 增强平台直播工具运行状态识别，结合程序路径、进程名和主窗口标题判断，并排除浏览器标题误判。
- OBS 由助手启动时跳过缺少文件检查；遇到异常退出提示时自动选择正常模式启动。
- 节目输出投影改为严格匹配，不再将场景、来源或多画面投影误认为节目输出。
- 加强 Windows 投影窗口的位置、尺寸和多屏布局恢复，增加启动后稳定校正。
- 减少开播检查期间的系统进程查询开销，避免 OBS 启动等待阻塞其他开播工具。

## 主要功能

- **OBS WebSocket 连接**
  - 支持配置主机、端口和 WebSocket 密码。
  - 支持连接测试，明确提示连接成功、端口错误、密码错误或输入源读取失败等情况。

- **多音源检测**
  - 从 OBS 输入源中筛选可能产生声音的来源。
  - 支持监听麦克风、声卡输入、系统音频、媒体源、浏览器源和采集设备等音频相关来源。
  - 支持同时选择多路音源，任一路被选中音源连续静音超时都会报警，适合主播、嘉宾、场外麦或主混音同时守护。

- **直播状态识别**
  - 默认在 OBS 正在直播、录制或开启虚拟摄像头时进入检测。
  - 未开播、未录制且未开启虚拟摄像头，或未选择音源、目标音源不存在、暂时没有电平数据时，不触发静音报警。

- **静音计时与报警**
  - 默认连续静音 120 秒后报警。
  - 静音达到设定时长的 75% 时显示预警浮窗。
  - 3 秒以内的短暂停顿仍视为正常讲话，超过 3 秒后进入静音计时。

- **小浮窗**
  - 支持将检测状态以小窗口形式置顶显示。
  - 小浮窗独立支持深色和浅色显示。
  - 正常状态显示“检测中 / 正在讲话 / 音频正常”，静音超过 3 秒后显示静音计时和剩余报警时间。
  - 提供音频提醒、音频与机位、多功能监看三种模式；多功能模式会根据启用模块数量自适应排版。
  - 支持按比例拖动缩放，并可选择显示 ATEM 当前机位、机位停留时间和 OBS 性能摘要。

- **报警弹窗**
  - 弹窗提供“确定”和“暂停检测”两种处理方式。
  - 点击“确定”后关闭弹窗，并重新开始静音计时。
  - 点击“暂停检测”后关闭弹窗并暂停后台检测，恢复时由用户主动开启。
  - 支持经典报警弹窗，或在桌面内容上方显示透明渐变红边并保留中央报警弹窗。
  - 可开启声音提示，通过系统默认输出设备播放提示音。

- **后台运行**
  - 关闭主窗口后程序继续在后台运行。
  - 需要完全退出时，可通过系统托盘或菜单栏图标退出。

- **开播检查**
  - 集中检查并启动开播所需的软件、快捷方式和指定网页。
  - Windows 支持自动发现程序、保存多屏窗口布局，并恢复本次新启动软件的位置。
  - 可在 OBS 启动后自动打开窗口化节目输出投影。

- **报警历史**
  - 本地保存最近 20 条报警记录。
  - 记录报警时间、音源名称、静音持续时间、用户操作和当时状态。

- **监控面板 Beta**
  - 显示 OBS `GetStats` 性能数据，包括 CPU、内存、FPS 和丢帧统计。
  - 显示所有音频输入设备及当前电平状态。
  - 显示最近音量历史和本次运行中的静音事件日志，便于直播后回溯。

- **ATEM 导播台**
  - 支持通过局域网连接 Blackmagic ATEM 导播台。
  - 可显示当前 PGM / PVW 机位和输入通道名称。
  - 支持扫描同网段疑似 ATEM 设备，并选择设备 IP。
  - 支持当前 PGM 机位停留计时，默认开启；同一机位连续 12 分钟未切换时触发正式报警。
  - 机位报警复用音频报警的窗口样式、提示音、多屏显示和处理按钮，并显示对应机位文案。
  - 本地记录每次 PGM 切换的来源、目标机位、切换时间和上一机位停留时长。
  - 开发者模式下可按直播场次查看机位切换时间轴与使用占比，本地自动保留最近 10 场。
  - 支持为常用信号设置自定义名称、颜色和分组。
  - 支持 Preview、AUTO Transition 与带确认保护的 Hard Cut。
  - 硬件连接默认关闭，可在完成网络与切台测试后按需启用。

- **软件更新**
  - 直播间内部服务器会提前缓存并校验 GitHub 最新安装包；客户端优先从内部服务器下载，缓存不可用时自动回退到镜像或 GitHub Releases。
  - Windows 版本会在后台准备更新，并在退出或下次启动时静默完成安装。
  - macOS 当前未进行 Apple Developer 签名，需要从发布页下载后手动替换 App。

- **直播间监控中心**
  - 软件启动后自动连接内部监控中心，无需扫码或额外开启。
  - 每个直播间对应一台检测电脑，统一上报 OBS、音频、ATEM、软件版本和更新状态。
  - 局域网线路可用时优先连接，外网环境自动使用公网 HTTPS。
  - 监控中心的直播间详情支持发送企业微信状态测试，验证机器人配置和当前数据。

## 首次配置

首次打开软件时，界面会显示新手引导。引导会按照实际界面位置说明关键区域，包括当前状态、OBS WebSocket 连接、端口与密码配置、连接测试、目标音源选择、报警规则、小浮窗和常用操作。普通版本更新不会重复显示引导；恢复出厂设置后会重新进入引导流程。

如需再次查看说明，可在软件中的“诊断测试”区域打开“查看说明书”。

## OBS 设置说明

1. 打开 OBS Studio。
2. 在 OBS 顶部菜单进入 `工具` -> `WebSocket 服务器设置`。
3. 启用 WebSocket 服务器。
4. 确认端口，OBS 默认端口通常为 `4455`。
5. 如 OBS 设置了 WebSocket 密码，请在本软件中填写相同密码。
   - 开启“安全保存 OBS 密码”时，软件会使用 Windows 凭据保护或 macOS 钥匙串加密保存。
   - macOS 询问“登录”钥匙串密码属于系统授权流程；不希望保存时可关闭该选项，密码只在本次运行中使用。
6. 回到本软件，点击连接测试，确认可读取 OBS 状态和输入源列表。
7. 在“目标音源”中选择需要守护的主播麦克风、无线领夹麦、声卡输入或直播主混音。需要同时监控多路麦克风时，可在设置中勾选多路音源。

OBS 28 及以上版本通常已内置 WebSocket 功能；旧版本可能需要单独安装 OBS WebSocket 插件。

## 日常使用流程

1. 启动 OBS 并确认 WebSocket 已启用。
2. 打开 OBS 音频检测助手。
3. 确认顶部状态显示 OBS 已连接。
4. 选择需要检测的目标音源。
5. 根据直播间节奏调整静音时长和静音阈值。
6. 开始直播或录制后，软件会自动进入检测。
7. 直播过程中可打开小浮窗，持续观察音频状态。

## 监控中心

1. 软件更新后首次启动时填写当前直播间名称。
2. 软件会自动选择局域网或公网线路并连接监控中心，无需手机配对。
3. 在电脑端“设置 → 连接与设备 → 直播间名称”中只需维护本机对应的直播间名称。
4. 在服务器 `/monitor` 中查看线路、延迟、最后同步时间和软件版本；进入直播间详情后可发送企业微信测试。

## ATEM 使用说明

1. 确认 ATEM 导播台和电脑位于同一局域网。
2. 在左侧进入 `ATEM 导播台`，点击 `配置 ATEM`。
3. 在设置中启用 `ATEM 连接`。
4. 如已知导播台 IP，可直接填写；如不确定，可点击 `查找导播台` 扫描同网段设备。
5. 连接成功后，主界面会显示 PGM、PVW 和输入通道名称。
6. 在 ATEM 页面中，数字键 `1-8` 可选择 PVW 机位，`Enter` 执行 AUTO 切换。启用全局切台快捷键后，该操作在其他软件中同样有效，直播前应先完成测试。
7. Hard Cut 默认带确认保护，建议在非直播环境测试快捷键和切换逻辑后再用于直播。

建议先在非直播环境中完成连接、快捷键和切换测试，再用于正式直播。

## 状态说明

| 状态 | 含义 |
| --- | --- |
| OBS 未连接 | 未能连接 OBS WebSocket，请检查 OBS 是否打开、WebSocket 是否启用、端口或密码是否正确。 |
| 等待直播/录制 | OBS 已连接，但当前未直播或录制，因此不会触发静音报警。 |
| 检测中 | OBS 已进入直播或录制，目标音源处于检测状态。 |
| 正在讲话 | 目标音源有有效电平，或短暂停顿未超过 3 秒。 |
| 静音计时中 | 目标音源低于阈值超过 3 秒，正在累计静音时长。 |
| 正在报警 | 目标音源连续静音达到设定时长，需要现场人员确认。 |
| 已暂停检测 | 用户主动暂停检测，恢复前不会触发新的报警。 |
| 检测已暂停 | 用户手动暂停检测，不会触发静音报警。 |

## 报警处理

- **确定**：确认本次报警，关闭弹窗，并重新开始静音计时。
- **暂停检测**：关闭弹窗并暂停检测，适合中场休息或调试设备；需要恢复时从主界面或托盘重新开启。

报警弹窗不会覆盖整个屏幕，方便导播在处理提醒的同时查看 OBS、直播后台或其他工作窗口。

## 适用场景

- 电商直播中主播麦克风没电、接收器断连或声卡输入异常。
- 导播需要同时观察 OBS、直播后台和音频检测状态。
- 长时间直播中，需要将音频异常转化为明确的现场提醒。
- 活动直播、访谈直播或培训直播中，需要持续确认主讲人音频是否正常。

## 数据与隐私

OBS 音频检测助手仅连接用户配置的 OBS WebSocket 与 ATEM 地址。报警历史保存在本地设备，不会上传至远程服务。

软件会主动向内部监控中心发送必要的状态摘要，包括直播间名称、当前音频源与电平、静音状态、OBS 运行摘要、ATEM PGM/PVW、机位计时、软件版本和更新状态。服务不会接收 OBS WebSocket 密码、报警历史或完整软件配置。

## 系统要求

- Windows 10 或 Windows 11
- macOS Apple Silicon 设备（M 系列芯片，arm64）
- 不支持 Intel x86 Mac
- OBS Studio 28 或更高版本
- OBS WebSocket 已启用

Windows 若关闭了系统“动画效果”，软件会遵循系统的减少动态效果设置。首次引导会检测并提示，可在 `设置` -> `辅助功能` -> `视觉效果` 中重新开启。

## 项目状态

该项目面向直播现场音频异常提醒场景持续维护。当前重点关注 OBS 音频源监听、静音报警、现场提示、小浮窗观察和 Windows 安装包交付。

## 开发文档

- [项目交接文档 (HANDOFF)](docs/HANDOFF.md)：完整架构、模块、开发、测试、构建、部署、运维与故障排查指南。
- [远程监控服务部署](docs/远程访问服务部署.md)：服务器部署与运维精简指南。
- [ATEM 导播台集成开发文档](docs/ATEM_导播台集成开发文档.md)：ATEM 功能设计与实现细节。
